package service.exceptions;

/**
 * Enumeration of error codes for the Player Service
 */
public enum ErrorCode {
    PLAYER_NOT_FOUND,
    INVALID_PLAYER_ID,
    INSUFFICIENT_FUNDS,
    INSUFFICIENT_SHARES,
    COMMUNICATION_ERROR
}